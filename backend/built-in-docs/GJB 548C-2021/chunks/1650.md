# 附录B (方法5010) (规范性附录) 晶圆批验收
## 方法5012 数字微电路的故障覆盖率测量
### 4.1.4 模型层次

---

逻辑模型可以是分层次的(即由宏模块组成), 也可以是单层次的(即没有宏模块的单层结构)。分层次并不在逻辑线上强加一些结构， 例如， 在宏输入或宏输出中没有隐含的扇出源。在模型中对应于物理分割的宏， 应采用附加的缓冲器(或等效的方法)强制加在实际器件的扇出上。